type HomeDict = Record<string, string>;

/**
 * Static quote cards removed (#667 honesty).
 * Re-enable when approved Review rows exist for homepage.
 */
export function HomeSocialProof({ dict: _dict }: { dict: HomeDict }) {
  return null;
}
